# `npm ci` installs from the lockfile inside the image, so a host node_modules would only
# slow the build down and can carry platform-specific binaries.
node_modules/
dist/
artifacts/
.env
.env.*
!.env.example
test-results/
playwright-report/
.idea/
.vscode/
